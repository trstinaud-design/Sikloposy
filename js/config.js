/* =========================================================================
   SIKLOPOSY — Adresse du serveur

   Remplissez ces deux valeurs UNE SEULE FOIS, puis gardez ce fichier avec
   l'application. Elles voyagent alors avec elle : sur un nouveau téléphone,
   il suffira de se connecter avec son e-mail et son mot de passe pour
   retrouver toutes ses données.

   Où les trouver : supabase.com → votre projet → Project Settings → API
     • supabaseUrl : « Project URL »        (ex. https://abcdefgh.supabase.co)
     • supabaseKey : clé « anon public »    (commence par eyJ…)

   La clé « anon » est prévue pour être publique : elle ne donne accès à rien
   sans compte. Ce sont les règles de sécurité du serveur (voir README) qui
   garantissent que chaque propriétaire ne voit que ses propres données.

   Laissé vide, l'application fonctionne en mode local uniquement.
   ========================================================================= */
window.CYCLOGEST_CONFIG = {
  supabaseUrl: '',
  supabaseKey: ''
};
